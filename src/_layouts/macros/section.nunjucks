
{% macro header(text, sub, icon) %}

<div>
  <h2 class="text-center">
    <i class="glyphicon glyphicon-{{ icon }}"  ></i>
    {{ text }}
    <br/>
    <small>{{ sub }}</small>
  </h2>
  <hr>
</div>

{% endmacro %}
